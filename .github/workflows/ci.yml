name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  apple:
    name: Xcode ${{ matrix.xcode }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Oldest supported toolchain (Swift 6.1, tools-version 6.0).
          - runner: macos-15
            xcode: "16.4"
          # Oldest Xcode 26.
          - runner: macos-26
            xcode: "26.0.1"
          # Newest available.
          - runner: macos-26
            xcode: "26.6"
    env:
      DEVELOPER_DIR: /Applications/Xcode_${{ matrix.xcode }}.app/Contents/Developer
    steps:
      - uses: actions/checkout@v5
      - name: Show toolchain
        run: swift --version
      - name: Test (macOS)
        run: swift test
      # The package has no platform-specific code, so one iOS build is enough to
      # show it resolves for iOS. Only the image's default Xcode ships the
      # simulator runtime this needs.
      - name: Build (iOS Simulator)
        if: matrix.xcode == '26.6'
        run: |
          xcodebuild build \
            -scheme MiAuth \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO

  linux:
    name: Linux Swift ${{ matrix.swift }}
    runs-on: ubuntu-latest
    container: swift:${{ matrix.swift }}
    strategy:
      fail-fast: false
      matrix:
        swift: ["6.0", "6.1", "6.2"]
    steps:
      - uses: actions/checkout@v5
      - name: Show toolchain
        run: swift --version
      - name: Test
        run: swift test
